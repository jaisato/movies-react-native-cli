/**
 * Local environment template.
 *
 * `src/utils/env.js` is git-ignored and is created from this file by the
 * `postinstall` script (`npm run setup:env` does the same) when it is missing.
 * Put your own TMDb API key there and never commit it.
 *
 * Get a free key at https://www.themoviedb.org/settings/api
 */
export const TMDB_API_KEY = '';
